diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -47,7 +47,7 @@ add_library(llama
 
 if (SLHA_INTEGRATION)
-    target_sources(llama PRIVATE slha_llama.cpp slha_replace_counters.cpp slha_layer_mask.cpp slha_score_scale.cpp slha_scale_fit.cpp slha_score_oracle.cpp slha_oracle_metrics.cpp slha_rank_dataset.cpp)
+    target_sources(llama PRIVATE slha_llama.cpp slha_external_k.cpp slha_replace_counters.cpp slha_layer_mask.cpp slha_score_scale.cpp slha_scale_fit.cpp slha_score_oracle.cpp slha_oracle_metrics.cpp slha_rank_dataset.cpp)
     target_compile_definitions(llama PRIVATE SLHA_INTEGRATION)
     target_include_directories(llama PRIVATE ${SLHA_INCLUDE_DIR})
     target_link_libraries(llama PRIVATE ${SLHA_LIB})
diff --git a/src/llama-kv-cache.cpp b/src/llama-kv-cache.cpp
--- a/src/llama-kv-cache.cpp
+++ b/src/llama-kv-cache.cpp
@@ -7,6 +7,7 @@
 
 #if defined(SLHA_INTEGRATION)
 #include "slha_llama.hpp"
+#include "slha_external_k.hpp"
 #endif
 
 #include <algorithm>
@@ -126,6 +127,8 @@ llama_kv_cache::llama_kv_cache(
     const uint32_t n_layer = hparams.n_layer_all;
 
 #if defined(SLHA_INTEGRATION)
+    const bool slha_external_k = slha_external_k_enabled();
+
     // Initialize before allocating any K tensor so the allocation hook sees
     // the exact model-derived layer count and can size the external tile store
     // on the first layer. Never infer the count later from graph callbacks.
@@ -136,7 +139,23 @@ llama_kv_cache::llama_kv_cache(
             throw std::runtime_error("failed to initialize SLHA integration");
         }
     }
+
+    if (slha_external_k) {
+        std::string error;
+        if (!slha_external_k_validate_environment(&error)) {
+            throw std::runtime_error("invalid SLHA external-K configuration: " + error);
+        }
+        if (n_stream != 1) {
+            throw std::runtime_error("SLHA external-K currently requires exactly one KV stream");
+        }
+        if (!slha_external_k_prepare_store(kv_size)) {
+            throw std::runtime_error("failed to size SLHA external-K tile store from llama.cpp KV capacity");
+        }
+    }
+#else
+    const bool slha_external_k = false;
 #endif
 
     // define a comparator for the buft -> ctx map to ensure that the order is well-defined:
@@ -260,18 +279,22 @@ llama_kv_cache::llama_kv_cache(
         const bool has_k = true;
         const bool has_v = !is_mla;
 
-        ggml_tensor * k = has_k ? ggml_new_tensor_3d(ctx, type_k, n_embd_k_gqa, kv_size, n_stream) : nullptr;
+        // In external-K mode keep only a constant-size metadata sentinel. The
+        // context-sized K payload lives exclusively in the SLHA tile store.
+        ggml_tensor * k = has_k
+            ? (slha_external_k
+                ? ggml_new_tensor_1d(ctx, type_k, 1)
+                : ggml_new_tensor_3d(ctx, type_k, n_embd_k_gqa, kv_size, n_stream))
+            : nullptr;
 #if defined(SLHA_INTEGRATION)
         // Keep GGML K storage unchanged; size the external SLHA tile store from
         // the runtime cache geometry at this allocation seam.
-        if (has_k) {
+        if (has_k && !slha_external_k) {
             slha_intercept_k_cache_allocation(k, kv_size, n_embd_k_gqa, n_stream);
         }
 #endif
         ggml_tensor * v = has_v ? ggml_new_tensor_3d(ctx, type_v, n_embd_v_gqa, kv_size, n_stream) : nullptr;
 
         has_k && ggml_format_name(k, "cache_k_l%d", il);
         has_v && ggml_format_name(v, "cache_v_l%d", il);
@@ -280,7 +303,9 @@ llama_kv_cache::llama_kv_cache(
         std::vector<ggml_tensor *> v_stream;
 
         for (uint32_t s = 0; s < n_stream; ++s) {
-            k_stream.push_back(has_k ? ggml_view_2d(ctx, k, n_embd_k_gqa, kv_size, k->nb[1], s*k->nb[2]) : nullptr);
+            k_stream.push_back(has_k && !slha_external_k
+                ? ggml_view_2d(ctx, k, n_embd_k_gqa, kv_size, k->nb[1], s*k->nb[2])
+                : nullptr);
             v_stream.push_back(has_v ? ggml_view_2d(ctx, v, n_embd_v_gqa, kv_size, v->nb[1], s*v->nb[2]) : nullptr);
         }
 
@@ -1191,6 +1216,12 @@ bool llama_kv_cache::get_can_shift() const {
     if (hparams.n_pos_per_embd() > 1) {
         return false;
     }
+#if defined(SLHA_INTEGRATION)
+    // Rotating compressed K after a logical position shift is not implemented.
+    // Refuse the operation rather than scoring stale pre-shift RoPE state.
+    if (slha_external_k_enabled()) {
+        return false;
+    }
+#endif
     return true;
 }
 
@@ -1252,6 +1283,18 @@ ggml_tensor * llama_kv_cache::get_k(ggml_context * ctx, int32_t il, uint32_t n_kv, const slot_info & sinfo) const {
 
     auto * k = layers[ikv].k;
 
+#if defined(SLHA_INTEGRATION)
+    if (slha_external_k_enabled()) {
+        const uint32_t ns = sinfo.s1 - sinfo.s0 + 1;
+        // Shape-only metadata consumed by the external score branch. This
+        // tensor is never expanded into the graph and therefore carries no K
+        // payload allocation.
+        return ggml_new_tensor_4d(ctx, GGML_TYPE_F32,
+                hparams.n_embd_head_k(il), hparams.n_head_kv(il), n_kv, ns);
+    }
+#endif
+
     const uint64_t kv_size      = get_size();
     const uint64_t n_embd_k_gqa = k->ne[0];
 
@@ -1377,6 +1420,14 @@ ggml_tensor * llama_kv_cache::cpy_k(ggml_context * ctx, ggml_tensor * k_cur, ggml_tensor * k_idxs, int32_t il, const slot_info & sinfo) const {
     }
 #endif
 
+#if defined(SLHA_INTEGRATION)
+    // The custom transform above is the complete K write in physical external
+    // mode. Expanding this returned node preserves graph ordering with the
+    // following attention op; no ggml_set_rows() into a persistent K tensor.
+    if (slha_external_k_enabled()) {
+        return k_cur;
+    }
+#endif
+
     const int64_t n_stream = k->ne[2];
 
     if (n_stream > 1) {
diff --git a/src/llama-graph.cpp b/src/llama-graph.cpp
--- a/src/llama-graph.cpp
+++ b/src/llama-graph.cpp
@@ -7,6 +7,7 @@
 
 #if defined(SLHA_INTEGRATION)
 #include "slha_llama.hpp"
+#include "slha_external_k.hpp"
 #endif
 
 #include "llama-kv-cache.h"
@@ -2384,12 +2385,20 @@ ggml_tensor * llm_graph_context::build_attn_mha(
                float   kq_scale,
                  int   il) const {
     const bool v_trans = v->nb[1] > v->nb[2];
+#if defined(SLHA_INTEGRATION)
+    const bool slha_external_k = slha_external_k_enabled();
+#else
+    const bool slha_external_k = false;
+#endif
 
     // split the batch into streams if needed
-    const auto n_stream = k->ne[3];
+    const auto n_stream = slha_external_k ? v->ne[3] : k->ne[3];
 
     q = ggml_view_4d(ctx0, q, q->ne[0], q->ne[1], q->ne[2]/n_stream, n_stream, q->nb[1], q->nb[2], q->nb[3]/n_stream, 0);
 
     q = ggml_permute(ctx0, q, 0, 2, 1, 3);
-    k = ggml_permute(ctx0, k, 0, 2, 1, 3);
+    if (!slha_external_k) {
+        k = ggml_permute(ctx0, k, 0, 2, 1, 3);
+    }
     v = ggml_permute(ctx0, v, 0, 2, 1, 3);
 
     ggml_tensor * cur;
@@ -2397,6 +2406,10 @@ ggml_tensor * llm_graph_context::build_attn_mha(
     const bool use_flash_attn = cparams.flash_attn && kq_b == nullptr;
     if (use_flash_attn) {
+#if defined(SLHA_INTEGRATION)
+        GGML_ASSERT(!slha_external_k && "SLHA external-K requires --flash-attn off");
+#endif
+
         GGML_ASSERT(kq_b == nullptr && "Flash attention does not support KQ bias yet");
 
         if (v_trans) {
@@ -2442,31 +2455,51 @@ ggml_tensor * llm_graph_context::build_attn_mha(
 
         cur = ggml_reshape_2d(ctx0, cur, cur->ne[0]*cur->ne[1], cur->ne[2]*cur->ne[3]);
     } else {
-        ggml_tensor * kq = ggml_mul_mat(ctx0, k, q);
-        cb(kq, "kq", il);
+        ggml_tensor * kq = nullptr;
 
-        // note: this op tends to require high floating point range
-        //       while for some models F16 is enough, for others it is not, so we default to F32 here
-        ggml_mul_mat_set_prec(kq, GGML_PREC_F32);
-
 #if defined(SLHA_INTEGRATION)
-        // Shadow/replace-mode: compare or substitute baseline logits with SLHA
-        // direct scores.  In shadow mode the op copies kq through unchanged and
-        // only records metrics; in replace mode the op overwrites the logits.
-        {
+        if (slha_external_k) {
             slha_layer_state * layer_state = slha_get_layer_state(il);
-            if (layer_state && layer_state->score_mode != SLHA_SCORE_OFF) {
-                kq = ggml_map_custom2(
-                    ctx0,
-                    kq,
-                    q,
-                    slha_shadow_score,
-                    GGML_N_TASKS_MAX,
-                    layer_state
-                );
-                cb(kq, "kq_slha_score", il);
-            }
+            GGML_ASSERT(layer_state != nullptr);
+            GGML_ASSERT(layer_state->score_mode == SLHA_SCORE_REPLACE);
+
+            // `k` is shape-only metadata in this branch: [head_dim,
+            // n_head_kv, n_kv, n_stream]. The seed owns only the ordinary
+            // transient logits shape; slha_shadow_score fills every row from
+            // the external tile store and never consumes baseline Q*K under
+            // the environment contract validated at cache construction.
+            ggml_tensor * seed = ggml_new_tensor_4d(
+                ctx0, GGML_TYPE_F32, k->ne[2], q->ne[1], q->ne[2], q->ne[3]);
+            kq = ggml_map_custom2(ctx0, seed, q, slha_shadow_score,
+                                  GGML_N_TASKS_MAX, layer_state);
+            cb(kq, "kq_slha_external", il);
+        } else
+#endif
+        {
+            kq = ggml_mul_mat(ctx0, k, q);
+            cb(kq, "kq", il);
+
+            // note: this op tends to require high floating point range
+            //       while for some models F16 is enough, for others it is not, so we default to F32 here
+            ggml_mul_mat_set_prec(kq, GGML_PREC_F32);
+
+#if defined(SLHA_INTEGRATION)
+            // Diagnostic/legacy replacement modes retain a real baseline KQ.
+            slha_layer_state * layer_state = slha_get_layer_state(il);
+            if (layer_state && layer_state->score_mode != SLHA_SCORE_OFF) {
+                kq = ggml_map_custom2(ctx0, kq, q, slha_shadow_score,
+                                      GGML_N_TASKS_MAX, layer_state);
+                cb(kq, "kq_slha_score", il);
+            }
+#endif
         }
-#endif
 
         if (arch == LLM_ARCH_GROK) {
             // need to do the following:
